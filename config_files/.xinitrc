setxkbmap -layout br -variant abnt2 &

# Check if HDMI-1 is connected and sets it up using a bash script generated by arandr
if xrandr | grep "HDMI-1 connected"; then
    # HDMI-1 is connected, execute commands accordingly
    ~/.screenlayout/2screen_home.sh &
else
    # HDMI-1 is not connected, execute alternative commands or nothing
    :
fi
/home/arthur/.fehbg & # This is background image
/home/arthur/housekeeping/status_bar_cfg.sh & # This is status bar
exec flameshot &
xbindkeys &
exec dwm
